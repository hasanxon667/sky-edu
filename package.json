{
  "name": "skyline-education-app",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "postinstall": "cd backend && npm install && npx prisma generate",
    "build": "cd backend && npm run build",
    "start": "cd backend && npm start"
  }
}
